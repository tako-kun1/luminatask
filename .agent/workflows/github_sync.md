---
description: Changes Sync (Add, Commit, Push)
---

1. Check current status
// turbo
2. Add all changes
// turbo
3. Commit with a timestamp message (You can edit this message)
   - `git commit -m "Update: $(Get-Date -Format 'yyyy-MM-dd HH:mm')"`
4. Push to main branch
   - `git push origin main`
