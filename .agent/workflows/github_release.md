---
description: Create New Version Release
---

1. Check current tags
   - `git tag`
2. Create a new tag (Edit the version number!)
   - `git tag v0.1.x`
3. Push the tag to trigger Auto-Updater build
   - `git push origin v0.1.x`
